int[10] foo (int x, int y, float i, char z, string x)
{
    int xx;
    float yy;
    char ll;
    string ss;
    int[20] xx;

    if (xx==(yy*ll+23-99*2))
    {
        xx[0] = ll<x;
        println "hello";
    }
    else
    {
        if (xx<yy+x-2*3)
        {
            return (x<y)==(6*7<xx);
        }
        while (x==2)
        {
            x = x+1;
            ;
            ;
            ;
        }
    }
}
